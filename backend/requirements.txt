# RefundPilot AI - backend dependencies
fastapi
uvicorn[standard]
pydantic>=2
python-dotenv
loguru

# Agent orchestration. If LangGraph fails to install on your Python version,
# the app automatically falls back to a built-in sequential orchestrator
# with the same node structure (see app/agent/graph.py).
langgraph
langchain-core

# LLM (optional). The app runs fully without any provider in deterministic mode.
# OpenAI (cloud) and/or local Ollama (via its HTTP API using requests).
openai
requests

# Testing
pytest
httpx
